import {
  CButton,
  CForm,
  CFormInput,
  CInputGroup,
  CInputGroupText,
  CModalBody,
  CModalFooter,
  // CModalHeader,
  // CModalTitle,
} from "@coreui/react";
import { cilLockLocked, cilUser } from "@coreui/icons";
import CIcon from "@coreui/icons-react";
import { ModalProps } from "../../../interfaces";

const UserDataModal = ({ closeModal, size, modalVisible }: ModalProps) => {
  return <></>;
};

export default UserDataModal;
